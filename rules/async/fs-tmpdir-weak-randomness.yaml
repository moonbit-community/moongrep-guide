package: moonbitlang/async/fs
description: |
  `@fs.tmpdir(...)` does not generate cryptographically strong directory names.
  Do not treat the returned path as a secret token or capability URL fragment.
patterns:
  - shape: |
      @fs.tmpdir()
  - shape: |
      @fs.tmpdir(prefix=__)
  - shape: |
      @fs.tmpdir(prefix~)
