package: moonbit-community/rabbita/svg
description: |
  SVG `<script>` content is active script. Treat any untrusted payload passed
  into `@svg.script(...)` as code execution.
patterns:
  - shape: |
      @svg.script([@svg.text(_script)])
    metavars: [_script]
