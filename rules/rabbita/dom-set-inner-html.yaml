package: moonbit-community/rabbita/dom
description: |
  Writing untrusted HTML into `set_inner_html(...)` is a direct DOM XSS sink.
patterns:
  - shape: |
      _node.set_inner_html(_html)
    metavars:
      subtree: [_node, _html]
